### endpoints the app actually serves
/          -> HTTP 200  body: {"hostname":"254c1c14a254","message":"hello from devops-assignment","version":"unknown"}
/health    -> HTTP 200  body: {"status":"ok"}
/healthz   -> HTTP 404  body: <!doctype html>
<html lang=en>
<title>404 Not Found</title>
<h1>Not Found</h1>
<p>The requested URL was not found on the

### what the target group is configured to check (terraform/alb.tf)
11:  port        = 5000
17:    path                = "/healthz"
28:  port              = 80

### what the app binds (app/app.py)
10:time.sleep(25)
29:    app.run(host="0.0.0.0", port=8080)

### listening sockets inside the container
8080 open: True
5000 open: False
